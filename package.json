{
    "dependencies": {
        "@tailwindcss/cli": "^4.1.17",
        "tailwindcss": "^4.1.17",
        "typescript": "^5.9.3"
    },

    "scripts": {
        "build:ts": "tsc",
        "build:css": "tailwindcss -i src/input.css -o dist/output.css --minify",
        "build": "npm run build:ts --silent && npm run build:css --silent"
    }
}
